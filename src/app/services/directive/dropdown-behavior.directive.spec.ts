import {DropdownBehaviorDirective} from './dropdown-behavior.directive';

describe('DropdownBehaviorDirective', () => {
  it('should create an instance', () => {
    // const directive = new DropdownBehaviorDirective();
    // expect(directive).toBeTruthy();
  });
});
