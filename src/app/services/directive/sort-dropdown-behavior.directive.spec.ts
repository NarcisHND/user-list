import {SortDropdownBehaviorDirective} from './sort-dropdown-behavior.directive';

describe('SortDropdownBehaviorDirective', () => {
  it('should create an instance', () => {
    // const directive = new SortDropdownBehaviorDirective();
    // expect(directive).toBeTruthy();
  });
});
